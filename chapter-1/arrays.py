# this is a data structure to collect different or the same type elements
# and it can contain any type of element, there is no restriction.
arr = [1, "a", 2.0, [1, 2]]

# Accessing an element time complexity - O(1)
# Inserting and deleting time complexity - O(n) because of replacing other elements